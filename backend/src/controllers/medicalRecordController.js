const MedicalRecordService = require('../services/medicalRecord.service');
const database = require('../database/database.json');

module.exports = class MedicalRecord{
    static async apiCreateMedicalRecord(request, response){
        
    }
}